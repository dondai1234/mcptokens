# mcptokens 0.1.3

Public-facing copy cleanup. The README on PyPI now:

- uses generic examples for the CLI (`python -m some_mcp_server`)
  instead of a project-specific server.
- states the actual current self-cost of **172 tokens** in the
  hero, with no future-frame promise.
- drops the test-result assertions from the published copy.

The product, install flow, and tool definition are unchanged.
`pip install mcptokens` from the canonical PyPI index continues to
land you at 172 tokens in your agent's harness.
